# Primary app database
DATABASE_URL=postgres://user:password@localhost:5432/jlang

# karasu-auth base URL used by the server-side auth proxy
PRIVATE_AUTH_BASE_URL=http://222.222.1.104:30025

# Rein Flashcard API base URL for server-to-server board sync.
# Use the public hostname unless you have a verified internal service route.
PRIVATE_FLASHCARD_API_URL=https://fc.nephren.xyz
PRIVATE_FLASHCARD_API_TIMEOUT_MS=3000

# Legacy alias still accepted by the server client
PRIVATE_FLASHCARD_BASE_URL=https://fc.nephren.xyz

# User-facing rein-flashcard app URL used in board study links
PUBLIC_FLASHCARD_APP_URL=https://fc.nephren.xyz
